# ntfy

Restored with fresh 5 GiB `local-bulk` storage on 2026-09-12 by J's explicit request. The former namespace and PVC were already absent; no legacy storage recovery was attempted. Chart and image versions are preserved.

The existing Application name, service name, and `https://ntfy.apps.home` route are retained. Auto-sync remains disabled; follow the repository's reviewed-sync protocol.
